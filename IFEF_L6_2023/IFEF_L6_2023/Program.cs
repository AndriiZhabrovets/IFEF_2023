// Euler's Project

static int ThreeOrFive(int TopNum)
{
    List<int> Res = new List<int>();

    for (int i = 0; i < TopNum; i++)
    {
        if (i % 3 == 0 || i % 5 == 0)
        {
            Res.Add()
        }
    }
    return Res;
}